#include<stdio.h>
int main(){
    printf("the number of Bytes in int is = %d\n",sizeof(int));
    printf("the number of Bytes in char is = %d\n",sizeof(char));
    printf("the number of Bytes in float is = %d\n",sizeof(float));
    printf("the number of Bytes in duble is = %d\n",sizeof(double ));
    printf("the number of Bytes in undigned int  is = %d\n",sizeof(unsigned int));
    printf("the number of Bytes in long int  is = %d",sizeof(long int));
    return 0;
}