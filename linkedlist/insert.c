#include<stdio.h>
#include<stdlib.h>

stuct Node {
    int data;
    stuct node* link;
}Node;
Node Insertlist(Node node){
    struct node NewLinked = (struct Node* )malloc(sizeof(struct Node));
    struct Node->data = data;
    struct Node->link=NUll;
}
void display(){
    printf("%d->",Node->data);
    
}

